// Arquivo: ControleDocenteFaltosos/backend/src/middlewares/auth.js
// Função: Verificar se usuário está autenticado

module.exports = (req, res, next) => {
  // Por enquanto, só libera (implementaremos depois)
  console.log('Middleware de autenticação - em desenvolvimento');
  next();
};